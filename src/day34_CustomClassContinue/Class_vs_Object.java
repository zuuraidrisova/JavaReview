package day34_CustomClassContinue;

public class Class_vs_Object {

    /*

   ~ class is a collection of similar objects, but object is an instance of a class
   ~ class is conceptual(template), but object is real
   ~ no memory is allocated for class, but each object has its own memory
   ~ class can exist without object, but object cannot exist without a class

   Memory:

        Stack vs  Heap:

         Stack is memory which contains methods, local variables, reference variables
         Heap is memory which contains objects, instance variables

     */
}
